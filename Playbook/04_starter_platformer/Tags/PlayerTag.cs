namespace MyGame.Platformer.Tags;

/// <summary>
/// Empty tag component that identifies the player entity.
/// Attach to an entity so systems can query for it specifically.
/// </summary>
public record struct PlayerTag;
