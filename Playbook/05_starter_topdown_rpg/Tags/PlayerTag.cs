namespace MyGame.TopDown.Tags;

/// <summary>
/// Tag component identifying the player entity.
/// Used to filter queries for player-specific systems (input, camera follow).
/// </summary>
public record struct PlayerTag;
