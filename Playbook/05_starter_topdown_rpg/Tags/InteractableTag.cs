namespace MyGame.TopDown.Tags;

/// <summary>
/// Tag component marking an entity as interactable.
/// Pair with <see cref="MyGame.TopDown.Components.Interactable"/> for radius and action data.
/// </summary>
public record struct InteractableTag;
